export interface Product {
    id: number;
    name: string;
    imageSrc: string;
    price: string;
    description: string;
}

export interface Testimonial {
    id: number;
    rating: number;
    name: string;
    review: string;
}